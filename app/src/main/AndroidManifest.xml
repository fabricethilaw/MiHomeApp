<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comfabricethilaw.mihome">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartHomeApp">
        <activity
            android:name="comfabricethilaw.mihome.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.SmartHomeApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="comfabricethilaw.mihome.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/SmartHome" />
    </application>

</manifest>